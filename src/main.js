import './style.css';
import { initializeApp } from './components/menu.js';

// Initialize the application when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
  initializeApp();
});